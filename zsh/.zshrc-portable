plugins=(zsh-completions)
autoload -U compinit && compinit

export PS1="%n %1~ %# "

export EDITOR='vim'

bindkey -v
export KEYTIMEOUT=1
bindkey "^R" history-incremental-search-backward

source ~/.aliases
