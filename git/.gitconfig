[user]
	name = Hugo Young
	email = hybas3@gmail.com
[alias]
	st = status
	ci = commit --verbose
	co = checkout
	di = diff
	dc = diff --cached
	amend = commit --amend
	aa = add --all
	ff = merge --ff-only
	pullff = pull --ff-only
	fa = fetch --all
	pom = push origin master
	ll = log --oneline
[rebase]
	autosquash = true
